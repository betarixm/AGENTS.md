---
title: React
---

## Rules

- Use functional components with hooks instead of class components.
- Do not annotate return types of React components; let TypeScript infer them.
- Consider extending existing component props instead of creating new ones when possible, to promote reusability and consistency.
